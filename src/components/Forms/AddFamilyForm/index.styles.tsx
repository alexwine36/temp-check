import React from 'react';
import {makeStyles, createStyles, Theme} from '@material-ui/core';

const AddFamilyFormStyles = makeStyles((theme: Theme) =>
  createStyles({
    
  })
);

export default AddFamilyFormStyles;