import React from 'react';
import {makeStyles, createStyles, Theme} from '@material-ui/core';

const InputFieldRendererStyles = makeStyles((theme: Theme) =>
  createStyles({
    
  })
);

export default InputFieldRendererStyles;