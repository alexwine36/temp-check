import React from 'react';
import {makeStyles, createStyles, Theme} from '@material-ui/core';

const TextInputFieldStyles = makeStyles((theme: Theme) =>
  createStyles({
    
  })
);

export default TextInputFieldStyles;