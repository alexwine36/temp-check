import React from 'react';
import {makeStyles, createStyles, Theme} from '@material-ui/core';

const AddFamilyPageStyles = makeStyles((theme: Theme) =>
  createStyles({
    
  })
);

export default AddFamilyPageStyles;