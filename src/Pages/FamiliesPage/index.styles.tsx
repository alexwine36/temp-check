import React from 'react';
import {makeStyles, createStyles, Theme} from '@material-ui/core';

const FamiliesPageStyles = makeStyles((theme: Theme) =>
  createStyles({
    
  })
);

export default FamiliesPageStyles;